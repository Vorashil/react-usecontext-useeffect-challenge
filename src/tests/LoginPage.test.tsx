
import React from 'react';
describe('LoginPage component', () => {
    test('renders login button if the user is not logged in', () => {
        //GIVEN
        //WHEN
        //THEN
    })
    test('renders logout button if the user is logged in', () => {
        //GIVEN
        //WHEN
        //THEN
    })
})